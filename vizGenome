#Align to genome
nucmer --maxmatch -p prefix -t 48 -p out genome1.fa final.genome.scf.fasta

#Convert into DOT format/ Download this script
#just download the DotPrep.py script and run it on the output from MUMmer's nucmer aligner, which will give you the same results as the Applet on DNAnexus.

https://dnanexus.github.io/dot/DotPrep.py

#Convert into the format
DotPrep.py --delta out.delta

#Upload the index and coords file at 
https://dnanexus.github.io/dot/
